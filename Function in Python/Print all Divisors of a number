from os import *
from sys import *
from collections import *
from math import *


def divisors(n):
    result = []
    for i in range(1, n+1):
        if n % i == 0:
            result.append(i)
    return result

n = int(input().strip())
print(*divisors(n))
